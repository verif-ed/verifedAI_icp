{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0",
    "yarn": ">=1.0.0"
  },
  "name": "verifedAI_icp",
  "scripts": {
    "build": "yarn run build --workspaces --if-present",
    "prebuild": "yarn run prebuild --workspaces --if-present",
    "pretest": "yarn run prebuild --workspaces --if-present",
    "start": "yarn start --workspaces --if-present",
    "test": "yarn test --workspaces --if-present"
  },
  "type": "module",
  "private": true,
  "workspaces": [
    "src/verifedAI_icp_frontend"
  ]
}